export const twoFer = (string name) => {
	  //
	  // YOUR CODE GOES HERE
	  //
	  if(name == "")
		  return ("One for you, one for me.');");
	  else
		  return ("One for {0}, one for me.');", name);
	
};