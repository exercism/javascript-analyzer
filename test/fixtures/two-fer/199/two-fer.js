export const twoFer = (name) => {
  if (name === '') {
    return name = 'One for you, one for me.';
  }
   if (name === 'Alice') {
     return name = 'One for Alice, one for me.';
   }
   else {
     return name = 'One for Bob, one for me.';
   }
};