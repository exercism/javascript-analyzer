var name = prompt("Insert name: ");
function twoFer() {
    if(name !== "") {
        console.log("One for " + name + ", one for me.");
    } else {
        console.log("One for you, one for me.");
    }
}

twoFer();