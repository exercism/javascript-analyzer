const twoFer = (name) => {
    if(name === true) {
        return 'one for ' + name + ' one for me'
    };
    
    else if(name === false) {
        return 'one for you one for me'
    };

    else {
        return 'error input'
    };
};

