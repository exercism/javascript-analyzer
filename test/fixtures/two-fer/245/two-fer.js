export function twoFer(){
   const  name = 'alice';
   const name_1= '';

  if(name = 'alice') {
  return 'One for Alice, one for me.';
  }
    else  {
      return  'One for you, one for me.';
          }
        }
    twoFer();
